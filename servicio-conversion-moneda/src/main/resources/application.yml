server:
  port: 8050

spring:
  application:
    name: servicio-conversion-moneda
  config:
    import: optional:configserver:http://localhost:8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: localhost

external:
  api:
    exchangerate:
      base-url: https://api.frankfurter.app